*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.*
 *
  Create expandable Exception. Set expandable Exception into the dialog pane.