        ca.uhn.fhir.model.api.Bundle bundle = fhirClient                .search()                .forResource(Patient.class)                .where(Patient.IDENTIFIER.exactly().systemAndIdentifier("urn:fake:mrns", "54321"))                .execute();        List<Patient> patients = bundle.getResources(Patient.class);        if(patients.size() > 1) {            for(Patient p : patients.subList(1, patients.size())) {                System.out.println(p.getId());                BaseOperationOutcome outcome = fhirClient                        .delete()                        .resourceById(p.getId())                        .execute();            }        }