 SPDX-License-Identifier: GPL-2.0
 * Copyright (c) 2018 MediaTek Inc.
  vlan port membership  egress mode  write to vlan table  set all ports as security mode  check if a port is used in tag/untag vlan egress mode  set all untag-only ports as transparent and the rest as user port  first clear the switch vlan table  now program only vlans with members to avoid
	 * clobbering remapped entries in later iterations
	  Port Default PVID  SPDX-License-Identifier: GPL-2.0
 * Copyright (c) 2018 MediaTek Inc.
  vlan port membership  egress mode  write to vlan table  set all ports as security mode  check if a port is used in tag/untag vlan egress mode  set all untag-only ports as transparent and the rest as user port  first clear the switch vlan table  now program only vlans with members to avoid
	 * clobbering remapped entries in later iterations
	  Port Default PVID  SPDX-License-Identifier: GPL-2.0
 * Copyright (c) 2018 MediaTek Inc.
  vlan port membership  egress mode  write to vlan table  set all ports as security mode  check if a port is used in tag/untag vlan egress mode  set all untag-only ports as transparent and the rest as user port  first clear the switch vlan table  now program only vlans with members to avoid
	 * clobbering remapped entries in later iterations
	  Port Default PVID 