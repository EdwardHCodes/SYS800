 SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		  SPDX-License-Identifier: GPL-2.0
 * Copyright (C) 2002 Roman Zippel <zippel@linux-m68k.org>
  Helper function to facilitate fgets() by Jean Sacren.  fall through  print help  fall through  fall through 
			 * Except in oldaskconfig mode, we show only menus that
			 * contain new symbols.
			  fall through  gcc uninit 
			 * syncconfig is invoked during the build stage.
			 * Suppress distracting "configuration written to ..."
			 
			 * Use microseconds derived seed,
			 * compensate for systems where it may be zero
			 zconfdump(stdout); Really nothing to do in this loop  fall through  Update until a loop caused no more changes 
		 * Create auto.conf if it does not exist.
		 * This prevents GNU Make 4.1 or older from emitting
		 * "include/config/auto.conf: No such file or directory"
		 * in the top-level Makefile
		 *
		 * syncconfig always creates or updates auto.conf because it is
		 * used during the build.
		 