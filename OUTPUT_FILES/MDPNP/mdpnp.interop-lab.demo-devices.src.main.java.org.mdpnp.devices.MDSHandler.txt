*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing*
 * @author mfeinberg
 *
 * Container for both sides. Each side can be instaciated separately if needs to
 *
     * Decorator for MDSConnectivity messages
     *
     * Decorator for MDSConnectivityObjective messages
     *
     * Decorator for MDSConnectivity messages
      for disconnected functionality - mostly for tests do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing do nothing