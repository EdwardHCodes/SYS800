 TODO Reorient?        currentContext = pane.canvas.getGraphicsContext2D();        currentContext.setFill(Color.RED);        currentContext.fillRect(x, y, width, height);        System.err.println("FILL RECT " + x + " " + y + " " + width + " " + height);        currentContext.setFill(Color.RED);        currentContext.clearRect(x, y, width, height);        currentContext.fillRect(x, y, width, height); TODO Reorient?        currentContext = pane.canvas.getGraphicsContext2D();        currentContext.setFill(Color.RED);        currentContext.fillRect(x, y, width, height);        System.err.println("FILL RECT " + x + " " + y + " " + width + " " + height);        currentContext.setFill(Color.RED);        currentContext.clearRect(x, y, width, height);        currentContext.fillRect(x, y, width, height); TODO Reorient?        currentContext = pane.canvas.getGraphicsContext2D();        currentContext.setFill(Color.RED);        currentContext.fillRect(x, y, width, height);        System.err.println("FILL RECT " + x + " " + y + " " + width + " " + height);        currentContext.setFill(Color.RED);        currentContext.clearRect(x, y, width, height);        currentContext.fillRect(x, y, width, height); TODO Reorient?        currentContext = pane.canvas.getGraphicsContext2D();        currentContext.setFill(Color.RED);        currentContext.fillRect(x, y, width, height);        System.err.println("FILL RECT " + x + " " + y + " " + width + " " + height);        currentContext.setFill(Color.RED);        currentContext.clearRect(x, y, width, height);        currentContext.fillRect(x, y, width, height);