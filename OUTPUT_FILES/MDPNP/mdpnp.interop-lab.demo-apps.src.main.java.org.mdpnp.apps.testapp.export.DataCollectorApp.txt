 hold on to the references so that we we can unhook the listeners at the end device list model maintains the list of what is out there. add a listener to it so that we can dynamically build a tree representation of that information. create a data filter - it will act as as proxy between the data collector and actual data consumers. all internal components with register with it for data events. add self as a listener so that we can show some busy data in the central panel. Is the current persister running? Stop it first. if current persister is running, stop it now. Add to the screen for visual.