 set for entity display ======================================================================= General routines =======================================================================Con_Print (text);
================
Sys_LowFPPrecision
================
 causes weird problems on Nextstep
================
Sys_HighFPPrecision
================
 causes weird problems on Nextstep !id386
===============================================================================

FILE IO

===============================================================================
static FILE	*sys_handles[MAX_HANDLES]; a file fully or partially cached in memory  We use the FILE handle to track the current position  NULL: unused slot  bytes 0-(resident_len-1) are in memory 
================
Qfilelength
================
 really rough guesses rb->splashf(HZ*2, "Allocating handle %d to %s", i, path); cache in memory?  cache all or part of it  fill in cache rb->splashf(HZ, "Close handle %d", handle); clear all fields so we can safely reuse partially or fully in memory  ======================================================================= Sleeps for microseconds =======================================================================	Sys_Warn("floating point exception\n");	signal(SIGFPE, floating_point_exception_handler);rb->splash(0, "quake 1");rb->splash(0, "quake 2");rb->splash(0, "quake 3");Cvar_RegisterVariable (&sys_nostdout);rb->splash(0, "quake 4"); find time spent rendering last frame play vcrfiles at max speed not time to run a server only tic yet profile only while we do each Quake frame graphic debugging aids
================
Sys_MakeCodeWriteable
================
 set for entity display ======================================================================= General routines =======================================================================Con_Print (text);
================
Sys_LowFPPrecision
================
 causes weird problems on Nextstep
================
Sys_HighFPPrecision
================
 causes weird problems on Nextstep !id386
===============================================================================

FILE IO

===============================================================================
static FILE	*sys_handles[MAX_HANDLES]; a file fully or partially cached in memory  We use the FILE handle to track the current position  NULL: unused slot  bytes 0-(resident_len-1) are in memory 
================
Qfilelength
================
 really rough guesses rb->splashf(HZ*2, "Allocating handle %d to %s", i, path); cache in memory?  cache all or part of it  fill in cache rb->splashf(HZ, "Close handle %d", handle); clear all fields so we can safely reuse partially or fully in memory  ======================================================================= Sleeps for microseconds =======================================================================	Sys_Warn("floating point exception\n");	signal(SIGFPE, floating_point_exception_handler);rb->splash(0, "quake 1");rb->splash(0, "quake 2");rb->splash(0, "quake 3");Cvar_RegisterVariable (&sys_nostdout);rb->splash(0, "quake 4"); find time spent rendering last frame play vcrfiles at max speed not time to run a server only tic yet profile only while we do each Quake frame graphic debugging aids
================
Sys_MakeCodeWriteable
================
 set for entity display ======================================================================= General routines =======================================================================Con_Print (text);
================
Sys_LowFPPrecision
================
 causes weird problems on Nextstep
================
Sys_HighFPPrecision
================
 causes weird problems on Nextstep !id386
===============================================================================

FILE IO

===============================================================================
static FILE	*sys_handles[MAX_HANDLES]; a file fully or partially cached in memory  We use the FILE handle to track the current position  NULL: unused slot  bytes 0-(resident_len-1) are in memory 
================
Qfilelength
================
 really rough guesses rb->splashf(HZ*2, "Allocating handle %d to %s", i, path); cache in memory?  cache all or part of it  fill in cache rb->splashf(HZ, "Close handle %d", handle); clear all fields so we can safely reuse partially or fully in memory  ======================================================================= Sleeps for microseconds =======================================================================	Sys_Warn("floating point exception\n");	signal(SIGFPE, floating_point_exception_handler);rb->splash(0, "quake 1");rb->splash(0, "quake 2");rb->splash(0, "quake 3");Cvar_RegisterVariable (&sys_nostdout);rb->splash(0, "quake 4"); find time spent rendering last frame play vcrfiles at max speed not time to run a server only tic yet profile only while we do each Quake frame graphic debugging aids
================
Sys_MakeCodeWriteable
================
