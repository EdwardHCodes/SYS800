*
 *
 *
     * @return true if all OK.
      Should check for valid RxO for endpoints using the same profile Should check for valid RxO for endpoints using the same profile*
 *
 *
     * @return true if all OK.
      Should check for valid RxO for endpoints using the same profile Should check for valid RxO for endpoints using the same profile*
 *
 *
     * @return true if all OK.
      Should check for valid RxO for endpoints using the same profile Should check for valid RxO for endpoints using the same profile*
 *
 *
     * @return true if all OK.
      Should check for valid RxO for endpoints using the same profile Should check for valid RxO for endpoints using the same profile