*
 * @author mfeinberg
 Observation o = emitter.fhirObservation(number);Assert.assertNotNull(o);            Assert.assertNotEquals(0, updates.size());Set<Observation> o = emitter.fhirObservation(assessment);Assert.assertNotNull(o);*
 * @author mfeinberg
 Observation o = emitter.fhirObservation(number);Assert.assertNotNull(o);            Assert.assertNotEquals(0, updates.size());Set<Observation> o = emitter.fhirObservation(assessment);Assert.assertNotNull(o);*
 * @author mfeinberg
 Observation o = emitter.fhirObservation(number);Assert.assertNotNull(o);            Assert.assertNotEquals(0, updates.size());Set<Observation> o = emitter.fhirObservation(assessment);Assert.assertNotNull(o);*
 * @author mfeinberg
 Observation o = emitter.fhirObservation(number);Assert.assertNotNull(o);            Assert.assertNotEquals(0, updates.size());Set<Observation> o = emitter.fhirObservation(assessment);Assert.assertNotNull(o);