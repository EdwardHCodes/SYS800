search through an image looking for header_string   printf("fail\n");return -1;search through an image looking for header_string   printf("fail\n");return -1;search through an image looking for header_string   printf("fail\n");return -1;