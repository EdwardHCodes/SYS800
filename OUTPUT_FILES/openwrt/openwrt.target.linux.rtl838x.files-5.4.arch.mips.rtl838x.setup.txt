 SPDX-License-Identifier: GPL-2.0-only
 * Setup for the Realtek RTL838X SoC:
 *	Memory, Timer and Serial
 *
 * Copyright (C) 2020 B. Koblitz
 * based on the original BSP by
 * Copyright (C) 2006-2012 Tony Wu (tonywu@realtek.com)
 *
  for mips_hpt_frequency  SoC reset vector (in flash memory): on RTL839x platform preferred way to reset  If calling reset vector fails, reset entire chip  If this fails, halt the CPU  Reset Global Control1 Register  Setup System LED. Bit 15 (14 for RTL8390) then allows to toggle it  UHI interface 
	 * Load the devicetree. This causes the chosen node to be
	 * parsed resulting in our memory appearing
	 
 * Linux clock API
  With the info from the command line and cpu-freq we can setup the console  SPDX-License-Identifier: GPL-2.0-only
 * Setup for the Realtek RTL838X SoC:
 *	Memory, Timer and Serial
 *
 * Copyright (C) 2020 B. Koblitz
 * based on the original BSP by
 * Copyright (C) 2006-2012 Tony Wu (tonywu@realtek.com)
 *
  for mips_hpt_frequency  SoC reset vector (in flash memory): on RTL839x platform preferred way to reset  If calling reset vector fails, reset entire chip  If this fails, halt the CPU  Reset Global Control1 Register  Setup System LED. Bit 15 (14 for RTL8390) then allows to toggle it  UHI interface 
	 * Load the devicetree. This causes the chosen node to be
	 * parsed resulting in our memory appearing
	 
 * Linux clock API
  With the info from the command line and cpu-freq we can setup the console  SPDX-License-Identifier: GPL-2.0-only
 * Setup for the Realtek RTL838X SoC:
 *	Memory, Timer and Serial
 *
 * Copyright (C) 2020 B. Koblitz
 * based on the original BSP by
 * Copyright (C) 2006-2012 Tony Wu (tonywu@realtek.com)
 *
  for mips_hpt_frequency  SoC reset vector (in flash memory): on RTL839x platform preferred way to reset  If calling reset vector fails, reset entire chip  If this fails, halt the CPU  Reset Global Control1 Register  Setup System LED. Bit 15 (14 for RTL8390) then allows to toggle it  UHI interface 
	 * Load the devicetree. This causes the chosen node to be
	 * parsed resulting in our memory appearing
	 
 * Linux clock API
  With the info from the command line and cpu-freq we can setup the console 