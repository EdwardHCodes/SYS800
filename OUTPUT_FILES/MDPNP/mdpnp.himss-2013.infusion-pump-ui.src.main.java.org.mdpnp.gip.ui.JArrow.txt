*
 * @author Jeff Plourde
 *
 		super.paintComponent(g);		g2d.clearRect(0, 0, size.width, size.height);*
 * @author Jeff Plourde
 *
 		super.paintComponent(g);		g2d.clearRect(0, 0, size.width, size.height);*
 * @author Jeff Plourde
 *
 		super.paintComponent(g);		g2d.clearRect(0, 0, size.width, size.height);*
 * @author Jeff Plourde
 *
 		super.paintComponent(g);		g2d.clearRect(0, 0, size.width, size.height);