 Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Inflate code taken from WikiViewer plugin by Adam Gashlin  Maximum number of bytes to process in one iteration  heap for gunzip  destination for uncompressed song  size of the song buffer  used size of the song buffer  for REPEAT_ONE we disable track limits ***************** rockbox interface ***************** this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  If couldn't get the whole buffer
        will trim file and put and 'end_command'
        at the end If is gzipped decompress it  Will use available remaining memory 
            as output buffer  Since metadata parser doesn't support VGZ 
             will set song length here  The main decoder loop  Set fade again in case we seek to start of song  Generate audio buffer  Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Inflate code taken from WikiViewer plugin by Adam Gashlin  Maximum number of bytes to process in one iteration  heap for gunzip  destination for uncompressed song  size of the song buffer  used size of the song buffer  for REPEAT_ONE we disable track limits ***************** rockbox interface ***************** this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  If couldn't get the whole buffer
        will trim file and put and 'end_command'
        at the end If is gzipped decompress it  Will use available remaining memory 
            as output buffer  Since metadata parser doesn't support VGZ 
             will set song length here  The main decoder loop  Set fade again in case we seek to start of song  Generate audio buffer  Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Inflate code taken from WikiViewer plugin by Adam Gashlin  Maximum number of bytes to process in one iteration  heap for gunzip  destination for uncompressed song  size of the song buffer  used size of the song buffer  for REPEAT_ONE we disable track limits ***************** rockbox interface ***************** this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  If couldn't get the whole buffer
        will trim file and put and 'end_command'
        at the end If is gzipped decompress it  Will use available remaining memory 
            as output buffer  Since metadata parser doesn't support VGZ 
             will set song length here  The main decoder loop  Set fade again in case we seek to start of song  Generate audio buffer 