 horrific and doesn't check overflow.  returns NULL when we're done otherwise returns input.  compile this with:
 *   gcc -o combi.exe -DSTANDALONE_COMBI_TEST combi.c malloc.c
  horrific and doesn't check overflow.  returns NULL when we're done otherwise returns input.  compile this with:
 *   gcc -o combi.exe -DSTANDALONE_COMBI_TEST combi.c malloc.c
  horrific and doesn't check overflow.  returns NULL when we're done otherwise returns input.  compile this with:
 *   gcc -o combi.exe -DSTANDALONE_COMBI_TEST combi.c malloc.c
 