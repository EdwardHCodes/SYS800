 Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Maximum number of bytes to process in one iteration ***************** rockbox interface ***************** for loop mode we disable track limits  t is track no to display  this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  Update internal track count  The main decoder loop  Generate audio buffer  Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Maximum number of bytes to process in one iteration ***************** rockbox interface ***************** for loop mode we disable track limits  t is track no to display  this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  Update internal track count  The main decoder loop  Generate audio buffer  Ripped off from Game_Music_Emu 0.5.2. http://www.slack.net/~ant/  Maximum number of bytes to process in one iteration ***************** rockbox interface ***************** for loop mode we disable track limits  t is track no to display  this is the codec entry point  we only render 16 bits  44 Khz, Interleaved stereo  this is called for each file to process  Read the entire file  Update internal track count  The main decoder loop  Generate audio buffer 