#define ZX_WRITE_OUT_TEXT TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings , int buflen    int statusbar_size = global_settings.statusbar ? STATUSBAR_HEIGHT : 0;, tmp, hlead = 0, hvowel = 0, htail = 0bool hangul = false; Copy default keyboard to buffer             param[l].curfont = FONT_UI; check if FONT_UI fits the screen  find max width of keyboard glyphs  Since we're going to be adding spaces, make sure that we check
         * their width too  Pad lines with spaces  We don't want to overflow the buffer  find max width for text string  calculate keyboard grid size  draw page  separator  write out the text  cursor  draw underbar  highlight the key that has focus  no dedicated flip key - flip page on wrap kbd_spellchar(param[l].kbd_buf[k]); no dedicated flip key - flip page on wrap  kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]); inserts the selected char  find input char #define ZX_WRITE_OUT_TEXT TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings , int buflen    int statusbar_size = global_settings.statusbar ? STATUSBAR_HEIGHT : 0;, tmp, hlead = 0, hvowel = 0, htail = 0bool hangul = false; Copy default keyboard to buffer             param[l].curfont = FONT_UI; check if FONT_UI fits the screen  find max width of keyboard glyphs  Since we're going to be adding spaces, make sure that we check
         * their width too  Pad lines with spaces  We don't want to overflow the buffer  find max width for text string  calculate keyboard grid size  draw page  separator  write out the text  cursor  draw underbar  highlight the key that has focus  no dedicated flip key - flip page on wrap kbd_spellchar(param[l].kbd_buf[k]); no dedicated flip key - flip page on wrap  kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]); inserts the selected char  find input char #define ZX_WRITE_OUT_TEXT TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings  TODO: Check keyboard mappings , int buflen    int statusbar_size = global_settings.statusbar ? STATUSBAR_HEIGHT : 0;, tmp, hlead = 0, hvowel = 0, htail = 0bool hangul = false; Copy default keyboard to buffer             param[l].curfont = FONT_UI; check if FONT_UI fits the screen  find max width of keyboard glyphs  Since we're going to be adding spaces, make sure that we check
         * their width too  Pad lines with spaces  We don't want to overflow the buffer  find max width for text string  calculate keyboard grid size  draw page  separator  write out the text  cursor  draw underbar  highlight the key that has focus  no dedicated flip key - flip page on wrap kbd_spellchar(param[l].kbd_buf[k]); no dedicated flip key - flip page on wrap  kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]);kbd_spellchar(param[l].kbd_buf[k]); inserts the selected char  find input char 